export default function Header({text}) {
    return (
        <div style={{background: "grey", textAlign: "center", marginBottom:"50px", marginTop:"0"}}>
            <h1>{text}</h1>
        </div>
    )
}